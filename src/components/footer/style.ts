import styled from "styled-components";

const FooterContainer = styled.footer`
  margin-left: 3rem;
  margin-top: 6rem;

  figure {
    img {
    height: 5rem;
    width: 7rem;
    }
  }
`;

export { FooterContainer }
